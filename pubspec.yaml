name: deserialize_union_types
description: A sample dart poc demonistrating how could we deal with union dart types
  while parsing to/from json.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.4 <3.0.0'


dependencies:
  path: ^1.8.0
  json_annotation:

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  build_runner:
  freezed:
  json_serializable:
