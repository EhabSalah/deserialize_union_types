A poc how could we use `freezed` and `json_serializable` to serialize and deserialize union types to/from
json, this would help us specially in parsing `graphql` response with a `union` type involved.
